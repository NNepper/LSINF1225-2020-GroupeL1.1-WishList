<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.uclouvain.lsinf1225.groupeL11.wishlist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".Interface.FollowWishlistItemsFragment"
            android:screenOrientation="portrait"/>

        <activity android:name=".Interface.ProfileCreationActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Interface.HomeActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Interface.RegisterActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Interface.LoginActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Interface.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>